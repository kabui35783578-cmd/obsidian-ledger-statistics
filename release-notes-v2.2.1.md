# 记账统计 v2.2.1

## AI 可理解交易用途
- 单笔大额异常会把触发交易的备注加入候选证据，帮助 AI 区分房租、押金、维修等不同用途。
- 分类支出增加、消费频率变化、单次花费变高和支出重心变化，只发送该分类三至五笔代表交易。
- 工资压力候选最多发送本周期金额最大的三笔交易；正常周期参考和无异常状态不发送交易备注。

## 隐私与安全边界
- 代表交易只包含日期、分类、金额和最长八十个字符的备注，不发送账本文件、文件路径或完整原始行。
- 交易备注被明确标记为不可信账目数据，只能作为用途线索，不能成为模型指令。
- AI 仍须引用有效证据；具体数字继续由程序展示，校验失败时回退到本地候选判断。

## 文档与验证
- 更新设置说明和 README，使 AI 实际发送范围与隐私说明保持一致。
- 新增候选事件备注范围、数量上限、路径隔离和 AI 输入测试；完整测试增至 62 项。

## 更新
BRAT 可从本 Release 获取独立的 main.js、manifest.json、styles.css，也可下载 ZIP 手动安装。
