# 记账统计 v2.2.0

## AI 洞察升级
- 程序负责计算工资周期、金额、分类参考、候选事件和证据，AI 负责选择真正值得关注的变化，并给出解释与行动建议。
- AI 现在可以生成标题、判断、行动建议及最多三个相关分类意见，不再只是选择本地固定文案。
- 没有可靠变化时，AI 会明确提示暂时无需调整；数据不足或置信度较低时必须表达不确定性。

## 事实与安全边界
- 每条 AI 判断必须引用程序提供的有效证据 ID，并至少引用所选候选事件的一条证据。
- 金额、日期、阿拉伯数字和百分比继续由程序展示，AI 文案不得自行添加；中文比例表达允许用于自然说明。
- 不存在的候选、证据或分类会被拒绝，并自动回退到本地候选判断。

## 刷新与界面
- 判断卡片增加独立的“建议”区域，同时保留右上角判断依据入口。
- 账目未变化时再次点击刷新不会重复请求 AI，直接保留当前判断并给出提示。
- 新版判断结构会使旧缓存自动失效并生成一次新结果。

## 验证
- 通过 61 项自动化测试和完整生产构建。
- 已在 Obsidian 中验证真实 AI 返回、缓存、无变化刷新及 390px 窄屏布局。

## 更新
BRAT 可从本 Release 获取独立的 main.js、manifest.json、styles.css，也可下载 ZIP 手动安装。
