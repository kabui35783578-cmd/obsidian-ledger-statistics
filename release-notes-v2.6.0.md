# 记账统计 v2.6.0

- 设置新增独立的“余额校准”主题：填写实际到账工资和当前余额，查看本周期已记账支出与正负“未记账净差额”。校准仅在当前工资周期生效；新发生的已记账消费继续扣减余额。
- 校准前已发生的消费后来补记时，只修正已记账支出与净差额，不从实际余额重复扣款。工资瀑布图明确标为“账面剩余”，与已校准的当前余额区分。
- 余额及净差额只用于对账展示，不进入消费异常、历史平均或 AI 洞察判断；不修改原始账本。
- 移动端洞察卡片的周期数据、分类参考及来源说明默认折叠，可按需展开，桌面端布局保持原样。
- 100 项自动测试通过，并检查插件重新加载和设置界面；实体手机尚未单独验证本次发布。

Release 包含供 BRAT 更新的独立 `main.js`、`manifest.json`、`styles.css`，以及手动安装 ZIP。
