# 记账统计 v2.4.0

## 洞察持续关注

- 已生成的 AI 洞察在同一工资周期内持续展示，只要原候选事件及关键依据仍然有效；卡片显示原判断的生成时间。
- 账目没有重要变化时，跨日不会再把有效洞察替换为“暂无新变化”，也不会重复调用 AI。
- 原事件明显变化或出现优先级更高的新事件时，在查看洞察卡片时自动重新评估；同一分类换一种说法不会机械地重复提醒。
- 原事件消失、关联交易备注变更、工资周期或关键统计依据改变时，旧判断立即撤下，显示最新本地候选判断并等待更新。
- 自动更新失败时保留仍有效的旧洞察，不对同一份未变化的数据反复请求。旧版缓存若无法核对历史依据，会在首次更新时重新生成。

## 验证与更新

- 新增跨日保留、异常消失、重要变化、新事件、网络失败、请求期间账目变动和旧缓存兼容的回归测试。
- BRAT 可获取本 Release 的独立 main.js、manifest.json、styles.css；ZIP 可用于手动安装。
