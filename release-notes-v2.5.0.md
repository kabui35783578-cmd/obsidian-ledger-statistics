# 记账统计 v2.5.0

- 总览新增工资周期瀑布图：工资设置值减去本周期全部已入账支出，分类扣减合计与当前剩余严格对账；已入账固定支出不会重复扣除。移动端使用逐级余额列表。
- 单分类明细新增单笔金额箱线图：以所选期间之前两个已结束工资周期为基准，至少 8 笔时展示历史四分位、离群值和本期最大单笔；移动端使用独立窄屏图形及可点击账目摘要。样本不足时不推断异常。
- 环形图最多显示六段，橙色突出最大分类、其余使用更清晰的灰阶；桌面端大环居中、图例分列两侧，长尾分类可展开。窄屏图例移到图下方。
- 96 项自动测试通过，并检查了桌面端实际界面和窄屏布局；尚未经过实体手机验证。

Release 包含供 BRAT 更新的独立 `main.js`、`manifest.json`、`styles.css`，以及手动安装 ZIP。
